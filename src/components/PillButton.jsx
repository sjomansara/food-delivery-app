import { Button, Form } from "react-bootstrap";

const PillButton = () => {
    return (
        <Form className="d-flex">
            <Button className="pillButton rounded-pill px-4 ms-2">Sign in</Button>
        </Form>
    );
  }
  
  export default PillButton;
  